# AGENTS.md

## Workflow Preferences

- All runnable scripts must include a clearly marked `USER SETTINGS` block at the top for tunable values.
- All scripts must be runnable through VS Code tasks. Do not require manual terminal commands.
- Use repository-relative defaults for file paths. Avoid hardcoded machine-specific absolute paths.
